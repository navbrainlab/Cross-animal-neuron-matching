# Public audit-package note

This is the data-bearing portion of the sealed main-table audit package. The
original package's embedded workspace code snapshot was removed because it
contained historical candidate models that are outside the public release
boundary. The predictions, metrics, manifests, references, protocol, and
repository-version provenance are retained. The maintained code that rescored
these artifacts is under `scripts/benchmarks/` in the repository root.
